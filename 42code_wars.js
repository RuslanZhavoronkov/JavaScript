var min = function(list){
    
    return Math.min.apply(null, list); //Возвращает минимальное число массива
}

var max = function(list){
    
    return Math.max.apply(null, list); //Возвращает максимальное число массива
}