function findDifference(a, b) {
  //loading...
  let [num1, num2, num3] = a;
  let [num4, num5, num6] = b;
  let mult1 = num1 * num2 * num3;
  let mult2 = num4 * num5 * num6;
  let result = mult1 - mult2;

  return mult1 < mult2 ? result * -1 : result;
}
